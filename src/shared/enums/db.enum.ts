/**
 * Enum representing the names of collections in the database.
 *
 * @enum {string}
 * @property {string} USER - Represents the 'users' collection.
 * @property {string} WORKSPACE - Represents the 'workspaces' collection.
 */
export enum DatabaseCollectionNames {
  USER = 'users',
  WORKSPACE = 'workspaces',
}
